{
  "name": "inferia-sso-smoke",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end SSO smoke test for InferiaLLM + inferia-auth (Playwright).",
  "scripts": {
    "smoke": "playwright test sso_smoke.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
